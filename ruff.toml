line-length = 120
target-version = "py312"

exclude = [
    "migrations",
    ".venv",
    "venv",
    "staticfiles",
]

[lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # line length is handled by the formatter; keep long strings readable
]
